name: COVID_19_MASK
description: 공적마스크 API를 통한 마스크 현황을 알아봅니다.

version: 1.0.0+3

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^0.5.24+1
  location: ^2.5.4
  flutter_polyline_points: ^0.1.0
  cupertino_icons: ^0.1.3
  json_annotation: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
